import React from 'react';
import { Container, Content, Header } from './styles';

const Admin: React.FC = () => {
  // const history = useHistory();
  return (
    <Container>
      <Content>
        <Header>Esta Página nao existe!</Header>
      </Content>
    </Container>
  );
};

export default Admin;
